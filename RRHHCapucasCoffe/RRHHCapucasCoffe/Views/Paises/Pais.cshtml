@model IEnumerable<RRHHCapucasCoffe.Models.Paises.Pais>

@{
    ViewData["Title"] = "Países";
}

<div class="card">
    
    <div class="card-header">
        <h5 class="card-title mx-3 mt-2 py-0">Paises</h5>
        <div class="d-flex" id="container-buttons">
            <div class="p-1 flex-fill">
                <div class="d-flex d-flex justify-content-end dt-buttons">
                    <button type="submit" class="d-flex align-items-center btn btn-outline-primary mx-1" onclick="location.href='@Url.Action("CrearPais", "Paises")'" title="Agregar">
                        <span>
                            <i class="bi bi-file-plus d-block d-lg-none"></i>
                            <span class="mx-1 d-none d-lg-block">Agregar</span>
                        </span>
                    </button>
                </div>
            </div>
        </div>
    </div>

    <div class="card-body mt-3">
        <div class="table-responsive">
            <table id="datatable" class="table dataTable">
                <thead>
                    <tr>
                        <th></th>
                        <th>Pais</th>
                        <th>Activo</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var pais in Model)
                    {
                        <tr>
                            <td>
                                <a class="btn btn-outline-primary" asp-action="EditarPais" asp-route-paisId="@pais.PaisId">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <a class="btn btn-outline-danger" asp-action="EliminarPais" asp-route-paisId="@pais.PaisId">
                                    <i class="bi bi-trash"></i>
                                </a>
                            </td>
                            <td>
                                @pais.PaisNombre
                            </td>
                            <td>
                                <div class="form-check form-switch">
                                    <input class="form-check-input" type="checkbox" disabled checked="@pais.PaisActivo">
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/table.js" asp-append-version="true"></script>
}
